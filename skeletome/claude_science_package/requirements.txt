# SKELETOME — scoring + aggregation dependencies
# Pinned to versions known-good as of 2026-07. Loosen only if Claude Science
# resolves a conflict; keep alphagenome and scipy/statsmodels pins intact.

# --- core numerics / IO ---
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
statsmodels==0.14.2
matplotlib==3.9.0

# --- genomics IO ---
pyBigWig==0.3.22        # phyloP / recomb bigWig reads (used upstream in P2; imported by comparator for neural bigWig fallback)
pysam==0.22.1           # optional: FASTA handling for local reference checks

# --- AlphaGenome hosted API (PRIMARY engine) ---
# Install from PyPI if published, else from GitHub. The mock scorer runs WITHOUT this.
# pip install alphagenome            # PyPI (preferred if available)
# pip install git+https://github.com/google-deepmind/alphagenome.git   # source fallback
alphagenome>=0.1.0 ; python_version >= "3.10"

# NOTE ON CHROMBPNET (optional enrichment layer, code/score_chrombpnet.py):
#   ChromBPNet + variant-scorer pin TensorFlow and are best run in the official
#   Docker image `kundajelab/chrombpnet:latest` or a SEPARATE conda env — they
#   conflict with the light env above. DO NOT add tensorflow here; keep this env
#   AlphaGenome-only so the mock/real AlphaGenome path stays installable in seconds.
#   See code/score_chrombpnet.py header for the isolated-env install recipe.
